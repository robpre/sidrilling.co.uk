{{#> layout }}
    <div class="row">
        <div class="columns">
            {{{contents}}}
        </div>
        <div class="columns">
            {{#each equipment}}
                {{> media_object this}}
            {{/each}}
        </div>
    </div>
{{/layout}}
