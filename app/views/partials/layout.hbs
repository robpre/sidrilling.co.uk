<!DOCTYPE html>
<html>
    {{> head}}
    <body>
        {{> header}}
        <main>
            <div class="scrollable content row">
                <div class="columns">
                    {{> @partial-block}}
                    {{{devPre this}}}
                </div>
            </div>
        </main>
        {{> footer}}
    </body>
</html>
